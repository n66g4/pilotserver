[{
  "step_title": "PilotServer Admin",
  "invalid_next_disabled": true,
  "items": [{
    "desc": "Set the admin password for /admin/. Optional fields can be filled later in the admin UI."
  }, {
    "type": "password",
    "desc": "Admin password (required, at least 8 characters)",
    "subitems": [{
      "key": "wizard_admin_password",
      "desc": "Password",
      "validator": {
        "allowBlank": false,
        "minLength": 8,
        "maxLength": 128
      }
    }]
  }, {
    "type": "textfield",
    "desc": "Optional public HTTPS URL (e.g. https://xxx.synology.me). Not DDNS setup; DDNS stays in DSM.",
    "subitems": [{
      "key": "wizard_public_base_url",
      "desc": "Public URL",
      "validator": {
        "allowBlank": true,
        "maxLength": 256
      }
    }]
  }, {
    "type": "multiselect",
    "desc": "Listen mode. Default is loopback only (127.0.0.1) behind DSM reverse proxy; check to allow direct LAN access at http://NAS-IP:18780/admin/",
    "subitems": [{
      "key": "wizard_allow_lan",
      "desc": "Allow direct LAN access (listen on 0.0.0.0:18780)",
      "defaultValue": false
    }]
  }, {
    "type": "password",
    "desc": "Optional pairing token (PILOTSERVER_PAIRING_TOKEN). Leave blank to only verify device register JWT.",
    "subitems": [{
      "key": "wizard_pairing_token",
      "desc": "Pairing token",
      "validator": {
        "allowBlank": true,
        "maxLength": 128
      }
    }]
  }]
}]
